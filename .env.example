VITE_API_URL=http://localhost:46460

# Vercel production value:
# VITE_API_URL=https://<your-public-api-hostname>
# This is the only frontend environment variable required. Keep database,
# JWT, Browser Use, Firecrawl, Supabase service-role, and model keys on the
# backend only.
